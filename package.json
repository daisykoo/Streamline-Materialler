{
  "name": "materialler",
  "version": "1.0.0",
  "description": "My material tool",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts": "tsc index.ts -w --outDir build",
    "start": "node --inspect ./build/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^10.3.2",
    "xlsx": "^0.13.0"
  }
}
